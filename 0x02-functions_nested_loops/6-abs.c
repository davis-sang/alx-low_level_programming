#include "main.h"
/**
*_abs - main function
*@j: an integer input
*Description: function returns the absolute vakue of a number
*Return: absolute value of a number
*/
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
